<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Wi-Fi addresses vary. SonarPlugin restricts ws:// to literal private/loopback
         IPs; public hosts still require wss://. XML cannot express IP ranges. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
