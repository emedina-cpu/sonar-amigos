# Configuración opcional incorporada al compilar la interfaz
VITE_SERVER_URL=wss://tu-servidor.example.com
# Para el servidor, pasar GROUP_KEY como variable de entorno del hosting.
# No subir una clave real a GitHub ni incorporarla a la APK.
